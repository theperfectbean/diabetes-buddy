# Root Archive

This directory contains historical planning, implementation, and status documents
that were previously stored in the repository root.

These files were moved to keep the root focused on runtime/build entrypoints and
core project configuration.
