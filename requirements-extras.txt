# Provider adapters and SDKs (optional)
openai
anthropic
ollama

# Embeddings and local model runtimes (optional overrides)
torch
transformers

# HuggingFace accelerated downloads (optional)
# huggingface-hub
